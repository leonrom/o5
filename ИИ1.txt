имею pOuts = new Set()
и sOuts ={  T: new Set(), L: new Set(), R: new Set(), B: new Set() } .

pOuts содержит обьекты pO5, имеющие поле pO5.scops={T, L, R, B}

Требуется для выборок for (const x of 'TLRB') сформировать (отсортированные) последовательности sOuts[x] содержащие pO5 из pOuts в порядке увеличения их значений pO5.scops[x]

мог бы написать на js достаточно быстрый но простой алгоритм?

Спасибо! Но есть пара уточнений.
1. "Из pOuts фильтруем те объекты" - не требуется, т.к. все объекты содержат заполненные scops
2. Ты выполняешь Array.from(pOuts), но мне кажется быстрее (и проще) было бы просто использовать (один, обнуляемый с фиксированным размером по pOuts.size) промежуточный Array, из которого потом просто записать в Set
